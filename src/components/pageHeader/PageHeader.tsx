import React from 'react';
import './pageHeader.scss';
const PageHeader = () => {
  console.log('pageHeader lendering');
  return <div className="page-header mb-4"></div>;
};

export default React.memo(PageHeader);
